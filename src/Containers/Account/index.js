import React from 'react';
import { Text, View } from 'react-native';

const AccountScreen = () => (
    <View style={{ flex: 1 }}>
        <Text>Account</Text>
    </View>
);

export default AccountScreen;
